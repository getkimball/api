[{features, [
    {counters, #{
        init => [
            #{pattern => ".*",
              type => bloom_scalable,
              size => 10000,
              error_probability => 0.01}
        ]
    }}
]}].
