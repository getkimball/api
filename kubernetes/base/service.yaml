---
apiVersion: v1
kind: Service
metadata:
  name: features
  namespace: getkimball
spec:
  selector:
    app: features
  ports:
    - protocol: TCP
      port: 80
      targetPort: api
      name: http
